このディレクトリには bitmap-mule で使うためのいくつかの bdf フォントが
含まれています。オリジナルは etl8x16-bitmap.bdf で、それ以外のファイル
は bdfresize(*1) を使って自動生成したものです。

インストールのやり方
--------------------

1) Makefile の FONTDIR を適当に修正して下さい。

2) `su' をタイプして root になって下さい。

3) `make install' とタイプして下さい。

   gzip で圧縮された pcf ファイルが使えないシステムでは、代わりに
   `make install-nogzip' とタイプする必要があります。


(*1) ftp://ftp.cs.titech.ac.jp/pub/X11/contrib/Local/bdfresize-1.4.tar.Z
